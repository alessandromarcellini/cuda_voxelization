COSE FATTE PER OTTIMIZZARE:
	1) setup_voxels => cambiato struttura grid e block da 3d a 1d per permettere accessi coalescenti nella scrittura.
	2) reset_voxels => levato num_points dalla struttura Voxel di modo da permettere accessi coalescenti in memoria nel reset a 0 dei num_points. usato un semplice array di interi per tenere traccia dei numeri di punti per voxel. => Non c'è più setup e reset voxels, solo una Memset
	3) cambiamento da int a short (int16) per le coordinate e numpoints nella struct Voxel
	4)
	
extract_active_voxels:
	1) atomicAdd eseguita da un solo thread per warp
	2) ILP fattore 16 (4 era ancora troppo poco) per aumentare le richieste di load contemporanee
	3) coalescing usando grid-stride
